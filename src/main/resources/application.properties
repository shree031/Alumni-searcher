spring.application.name=alumni-searcher

# PostgreSQL DB Config
spring.datasource.url=jdbc:postgresql://localhost:5432/alumni_db
spring.datasource.username=postgres
spring.datasource.password=7001
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#PhantomBuster
phantombuster.api.key=dummy-api-key
phantombuster.phantom.id=dummy-phantom-id
phantombuster.output.url=https://api.phantombuster.com/api/v2/containers/fetch-output
phantombuster.session.cookie=dummy-cookie
phantombuster.identity.id=dummy.id
phantombuster.identity.name=Shreedevi Patil
